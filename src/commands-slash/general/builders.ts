import ping from "./ping/builder";
import turntable from "./turntable/builder";
import expression from "./expression/builder";

const builders = {
  ping,
  turntable,
  expression,
};

export default builders;
